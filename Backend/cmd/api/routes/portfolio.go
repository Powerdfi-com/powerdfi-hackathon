package routes

// func setPortfolioRoutes(app internal.Application, engine *echo.Echo) {
// 	handler := handlers.NewPortfolioHandler(app)
// 	portfolio := engine.Group("portfolio")

// 	portfolio.PATCH("", handler.Update, middleware.Authentication(app), middleware.RequireActivated)
// 	portfolio.GET("", handler.Get, middleware.Authentication(app))

// }
